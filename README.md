# SportTrackPro
Gestione società sportive con Supabase.

## Setup
1. Crea progetto Supabase, abilita Auth (Email).
2. Esegui `supabase.sql` nel SQL Editor.
3. Deploy su Netlify/Vercel con env vars da `.env.example`.
4. Registrati, modifica `role='admin'` nel DB.

Accesso controllato da RLS.
